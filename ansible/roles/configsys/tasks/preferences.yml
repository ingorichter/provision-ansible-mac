---
- name: Sys Config | System Preferences | General
  shell: "{{ item }}"
  with_items: 
   - "{{ osx_config_sys.system_preferences.line_1.general }}"
  become: true

- name: Sys Config | System Preferences | Desktop & Screen Saver
  shell: "{{ item }}"
  with_items: 
   - "{{ osx_config_sys.system_preferences.line_1.desktop_screen_saver }}"
  become: true

- name: Sys Config | System Preferences | Dock
  shell: "{{ item }}"
  with_items: 
   - "{{ osx_config_sys.system_preferences.line_1.dock }}"
  become: true

- name: Sys Config | System Preferences | Mission Control
  shell: "{{ item }}"
  with_items: 
   - "{{ osx_config_sys.system_preferences.line_1.mission_control }}"
  become: true

# - name: Sys Config | System Preferences | Language & Region
#   shell: "{{ item }}"
#   with_items: 
#    - "{{ osx_config_sys.system_preferences.line_1.language_region }}"
#   become: true

- name: Sys Config | System Preferences | Security & Privacy
  shell: "{{ item }}"
  with_items: 
   - "{{ osx_config_sys.system_preferences.line_1.security_privacy }}"
  become: true

- name: Sys Config | System Preferences | Spotlight
  shell: "{{ item }}"
  with_items: 
   - "{{ osx_config_sys.system_preferences.line_1.spotlight }}"
  become: true

- name: Sys Config | System Preferences | Energy Saver
  shell: "{{ item }}"
  with_items: 
   - "{{ osx_config_sys.system_preferences.line_2.energy_saver }}"
  become: true

- name: Sys Config | System Preferences | Keyboard
  shell: "{{ item }}"
  with_items: 
   - "{{ osx_config_sys.system_preferences.line_2.keyboard }}"
  become: true

- name: Sys Config | System Preferences | Network
  shell: "{{ item }}"
  with_items: 
   - "{{ osx_config_sys.system_preferences.line_3.network }}"
  become: true

# - name: Sys Config | System Preferences | Sharing
#   shell: "{{ item }}"
#   with_items: 
#    - "{{ osx_config_sys.system_preferences.line_3.sharing }}"
#   become: true

- name: Sys Config | System Preferences | App Store
  shell: "{{ item }}"
  with_items: 
   - "{{ osx_config_sys.system_preferences.line_4.app_store }}"
  become: true

# - name: Sys Config | System Preferences | Date & Time
#   shell: "{{ item }}"
#   with_items: 
#    - "{{ osx_config_sys.system_preferences.line_4.date_time }}"
#   become: true

- name: Sys Config | System Preferences | Time Machine
  shell: "{{ item }}"
  with_items: 
   - "{{ osx_config_sys.system_preferences.line_4.time_machine }}"
  become: true
